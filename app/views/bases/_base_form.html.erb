<%= form_with(model: Bases, url: bases_path, local: true, method: :post) do |f| %>
            <%= render 'shared/error_messages', object: @bases %>

            <%= f.label :base_number, class: "label-#{yield(:class_text)}" %>
            <%= f.text_field :base_number, class: "form-control" %>

            <%= f.label :base_name, class: "label-#{yield(:class_text)}" %>
            <%= f.text_field :base_name, class: "form-control" %>

            <%= f.label :time_type, class: "label-#{yield(:class_text)}" %>
            <%= f.time_field :time_type, class: "form-control" %>

            <div class="center">
              <%= f.submit yield(:button_text), class: "btn btn-primary btn-#{yield(:class_text)}" %>
            </div>
          <% end %>